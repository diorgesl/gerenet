import { PageHeader } from "@/components/PageHeader";

// Stub do ciclo D1 — a página de lista real (tabela, filtros, formulário de
// criação) chega na D2 da fase 5 (upstreams BGP, spec §7).
export default function Upstreams() {
  return (
    <div>
      <PageHeader titulo="Upstreams" sub="Conectividade própria — trânsito, IX, PNI e contingência." />
      <p>Página em construção — a fase 5 (upstreams BGP) está em andamento.</p>
    </div>
  );
}
